# Customer URL Compatibility

The public tracking app now lives at the repository root:

- `../index.html` — public landing page
- `../track.html` — shipment lookup and result page

These customer URLs remain as redirects for existing bookmarks and links.
